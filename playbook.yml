---
# Run with: `ansible-playbook -i inventory/inventory.ini [-b] [--ask-become-pass] -v playbook.yml`
# Using a vault: `ansible-playbook -i inventory/inventory.ini --ask-vault-pass -e "@auth.yml" -v playbook.yml`

# Creating roles: https://galaxy.ansible.com/docs/contributing/creating_role.html

- name: "Default Playbook"
  hosts:
    # List groups from your inventory here
    # You could also use the built in "all" or "ungrouped"
    # "all" is necessary when Vagrant is auto-generating the inventory
    #all
    localgroup
    remotegroup
  roles:
    # List roles to run
#    - role: "update-packages"
#    - role: "install-unbound"
#    - role: "install-ykman"
#    - role: "install-chrome"
#    - role: "build-wireguard-server"
#    - role: "build-tailscale-node"
#    - role: "install-auditd"
#    - role: "aide"
